{
  "name": "@authwrite/hono",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Edge-compatible Hono middleware adapter for AuthEngine.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/richardadalton/authwrite.git",
    "directory": "packages/hono"
  },
  "keywords": ["authorization", "authz", "hono", "edge", "workers"],
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "test -d dist && echo 'dist already built, skipping' || (npm run clean && npm run build)"
  },
  "dependencies": {
    "@authwrite/core": "*"
  },
  "peerDependencies": {
    "hono": ">=4.0.0"
  },
  "devDependencies": {
    "hono": "^4.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
