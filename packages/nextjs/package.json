{
  "name": "@daltonr/authwrite-nextjs",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Next.js route handler wrapper for AuthEngine.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/richardadalton/authwrite.git",
    "directory": "packages/nextjs"
  },
  "keywords": ["authorization", "authz", "nextjs", "app-router"],
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "test -d dist && echo 'dist already built, skipping' || (npm run clean && npm run build)"
  },
  "dependencies": {
    "@daltonr/authwrite-core": "*"
  },
  "peerDependencies": {
    "next": ">=14.0.0"
  },
  "devDependencies": {
    "next": "^15.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
