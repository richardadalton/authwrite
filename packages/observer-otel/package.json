{
  "name": "@daltonr/authwrite-observer-otel",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "OpenTelemetry spans and deny-rate metrics observer for AuthEngine.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/richardadalton/authwrite.git",
    "directory": "packages/observer-otel"
  },
  "keywords": ["authorization", "authz", "observer", "opentelemetry", "otel", "metrics"],
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "prepublishOnly": "test -d dist && echo 'dist already built, skipping' || (npm run clean && npm run build)"
  },
  "dependencies": {
    "@daltonr/authwrite-core": "*"
  },
  "peerDependencies": {
    "@opentelemetry/api": ">=1.0.0"
  },
  "devDependencies": {
    "@opentelemetry/api": "^1.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
